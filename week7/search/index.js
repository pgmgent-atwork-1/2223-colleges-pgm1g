(async () => {
  const params = new URLSearchParams(window.location.search);
  const search = params.get("search");

  // Stap 1: fetchen van alle events
  // ...

  // Stap 2: events filteren op basis van de search string
})();
